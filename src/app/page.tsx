import Hero from "@/components/Hero";

export default async function Home() {
  return (
    <section className="text-white h-full flex justify-center items-center">
      <Hero />
    </section>
  )
}
